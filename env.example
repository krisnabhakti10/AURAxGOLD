# ============================================================
# AURAxGOLD Licensing Portal — Environment Variables
# Copy this file to .env.local and fill in the values.
# NEVER commit .env.local to version control.
# ============================================================

# Supabase Project URL (from Supabase Dashboard > Project Settings > API)
SUPABASE_URL=https://your-project-id.supabase.co

# Supabase Service Role Key (Secret — NEVER expose to client)
# Found in: Supabase Dashboard > Project Settings > API > service_role key
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Admin Portal Password (used for /admin page and /api/admin/* routes)
ADMIN_PASSWORD=your-strong-admin-password-here

# EA Verification API Key (sent as x-ea-key header from MT5 EA)
# Generate a strong random string, e.g.: openssl rand -hex 32
EA_VERIFY_API_KEY=your-ea-verify-api-key-here
